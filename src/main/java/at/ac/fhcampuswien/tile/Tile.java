package at.ac.fhcampuswien.tile;

import java.awt.image.BufferedImage;

// Failed Class. No use found in it
// Can't delete because Manager-Class would need rewrite. Lazy Coding.

public class Tile {
    public BufferedImage image;
    public boolean collision = false;       //collision is false as default
}
